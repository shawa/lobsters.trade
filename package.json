{
  "scripts": {
    "build": "elm make src/Main.elm --output=public/elm.js --optimize",
    "live": "elm-live src/Main.elm --hot --debug --dir=public -- --output=public/elm.js"
  },
  "dependencies": {
    "elm": "^0.19.1-3"
  },
  "devDependencies": {
    "elm-live": "^4.0.2"
  }
}
